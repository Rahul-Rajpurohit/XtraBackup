NOTE:- 
To run any of the shell scripts, use the commands -
chmod +x <filename>.sh
sudo ./<filename>.sh

1. Run the script labeled "xtrainstall.sh".

*For all the other files,
--Change the username from "rahulraj" to custom username in all the backup directories.
--Change --user and --password to the targets MySQL credentials.
--Change the number of incremental backup as per the requirement.

	
